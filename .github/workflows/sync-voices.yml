name: Sync voice assets

on:
  schedule:
    # 5h 31m after the 11:00 UTC Global reset; allow post-patch mirrors to update.
    - cron: '31 16 * * *'
  workflow_dispatch:

concurrency:
  group: sync-voice-assets
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 1
          sparse-checkout: |
            .github
            scripts

      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - name: Install FFmpeg
        run: |
          sudo apt-get update
          sudo apt-get install --yes ffmpeg

      - name: Sync changed clips from the primary public mirror
        run: python3 scripts/sync_voice_assets.py

      - name: Sync changed clips from the Aceship Global mirror
        run: python3 scripts/sync_supplemental_voice_assets.py

      - name: Commit synced assets
        run: |
          if git diff --cached --quiet; then
            echo 'No voice asset changes.'
            exit 0
          fi
          git config user.name 'arkpedia-bot'
          git config user.email 'arkpedia-bot@users.noreply.github.com'
          git commit -m 'Sync voice assets from public upstream'
          git push origin main
